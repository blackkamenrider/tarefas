import { AndroidPickerMode, AndroidPickerProps, DateTimePickerResult } from '../types';
declare const getPicker: (mode: AndroidPickerMode) => (props: AndroidPickerProps) => Promise<DateTimePickerResult>;
export { getPicker };
