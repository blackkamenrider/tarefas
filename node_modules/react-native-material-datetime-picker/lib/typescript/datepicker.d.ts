import type { DateTimePickerResult, DatePickerOptions } from './types';
export default class DatePicker {
    static show(options: DatePickerOptions): Promise<DateTimePickerResult>;
    static dismiss(): Promise<boolean>;
}
