import { useEffect } from 'react';
import { AndroidPickerMode } from './types';
import { MaterialDatetimePickerAndroid } from './utils/MaterialDatetimePickerAndroid';

const RNMaterialDatetimePicker = props => {
  const {
    mode = AndroidPickerMode.DATE
  } = props;
  useEffect(() => {
    return () => {
      MaterialDatetimePickerAndroid.dismiss();
    };
  }, [mode]);
  useEffect(() => {
    MaterialDatetimePickerAndroid.show(props); // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [mode]);
  return null;
};

export default RNMaterialDatetimePicker;
//# sourceMappingURL=datetimepicker.android.js.map