import pickers from '../picker';
import { AndroidPickerMode } from '../types';

const getPicker = mode => {
  switch (mode) {
    case AndroidPickerMode.TIME:
      {
        return props => pickers[mode].show(props);
      }

    case AndroidPickerMode.DATE:
    default:
      {
        return props => pickers.date.show(props);
      }
  }
};

export { getPicker };
//# sourceMappingURL=android.js.map